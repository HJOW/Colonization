<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 기본설정 참고 : https://lavender1122.tistory.com/115 -->
    <settings>
    
        <!-- 데이터 등록시 속성값이 존재하지 않으면 (즉, NULL) 
        부적합한 열유형 1111 예외 발생함. 이 경우 해당타입을 명시적으로 알려주던지
        아래와 같이 NULL인 속성에 대하여 특정값으로 자동 설정이 가능함. -->
        <setting name="jdbcTypeForNull" value="NULL"/>
        
        <!-- DB 컬럼명을 낙타표기법으로 속성명과 자동 매핑 설정하기 -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
    
    </settings>
    
    <typeAliases></typeAliases>

    <environments default="dev">
        <environment id="dev">
            <transactionManager type="JDBC" />
            <dataSource type="POOLED">
                <property name="driver"   value="org.h2.Driver"   />
                <property name="url"      value="jdbc.h2.mem:coldb;DB_CLOSE_DELAY=-1" />
                <property name="username" value="sa"              />
                <property name="password" value=""                />
                <property name="poolMaximumActiveConnections" value="2"/>
                <property name="poolMaximumIdleConnections"   value="2"/>
            </dataSource>
        </environment>
    </environments>
    
    <mappers>
        <mapper resource="mapper/member.xml"/>
    </mappers>
</configuration>